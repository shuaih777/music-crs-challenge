# Pinned to versions known-cached on lrl-pypi-remote-cache.
# These same versions work on any standard PyPI mirror.
datasets==2.19.0
pandas==2.2.3
numpy==2.2.6
scipy==1.16.0
tqdm==4.67.3
huggingface_hub==0.26.2
pyarrow==20.0.0
pyarrow_hotfix==0.6
multiprocess==0.70.17
typing_extensions>=4.12

# LightGBM (CPU, for learned-to-rank reranker)
lightgbm>=4.0

# Gensim (CPU, for Item2Vec Word2Vec training)
gensim>=4.3
