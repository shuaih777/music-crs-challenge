# GPU-only requirements. Install in addition to requirements.txt.
# Pick the torch wheel matching your CUDA version.

torch>=2.3
transformers>=4.45,<5.0
sentence-transformers==3.3.1
accelerate>=0.30
peft>=0.12
einops

# Optional but recommended:
# flash-attn          # uncomment after installing torch; needs nvcc
# bitsandbytes        # for 4-bit/8-bit model loading
# vllm                # for fast listwise reranker inference
